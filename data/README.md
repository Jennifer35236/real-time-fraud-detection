# Dataset

This project expects the credit-card fraud dataset in a local file named `creditcard.csv`.

The recovered implementation expects the columns:

- `Time`
- `V1` through `V28`
- `Amount`
- `Class`

The dataset is intentionally not committed to this repository.

Place `creditcard.csv` in the repository root before running the training or producer scripts.
